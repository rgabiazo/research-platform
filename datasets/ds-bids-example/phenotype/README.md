# phenotype

`toy_phenotype.tsv` is a deterministic, hand-authored toy table whose identifiers align only with
the synthetic `participants.tsv` fixture. Every value is fictional; the table contains no
participant-derived data or copied study metadata.

Keep future fixtures equally small and synthetic. Do not commit raw study exports or private
participant metadata here.
