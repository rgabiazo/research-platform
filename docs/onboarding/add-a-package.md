
# Add a package

1. Copy `templates/package-template/`
2. Rename the directory under `packages/`
3. Update `pyproject.toml`
4. Add namespace package code under `src/research_platform/...`
5. Add tests, docs, and examples
6. Register the package in `WORKSPACE.yaml` if helpful
