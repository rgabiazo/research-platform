
# ops

Environment definitions, containers, SLURM profiles, sync helpers, CI notes,
and operational conventions live here.

`ops/secrets/` contains templates and policy notes only.
Real secrets belong in the top-level `secrets/` directory and should remain untracked.
