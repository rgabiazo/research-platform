# Synthetic nonrecognition fixtures

The source CSVs and expected event tables are deterministic, hand-authored toy data. The `007` and
`sub-007` labels are fictional test identifiers, not participant identifiers, and no value was
derived from a participant or study export. These fixtures are duplicated at the package boundary
to verify the public event-building integration.
