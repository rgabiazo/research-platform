# Package template

Use this as a structural starting point for a separately owned package within
the coordinated workspace. Keep reusable logic in the package and declare its
dependencies and public boundaries explicitly.

Replace placeholder names, update metadata, add tests, and document boundaries clearly.
